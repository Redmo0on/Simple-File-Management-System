# File Management System (Notes App) 📂

A modern, responsive note-taking application built with **Python (Flask)** and **Vue.js 3**.
Features a beautiful "Glassmorphism" UI with Tailwind CSS.

## Features ✨

- **Create Notes**: Add new notes with titles and content.
- **View Notes**: See all your notes in a responsive grid layout.
- **Edit Notes**: Update existing notes in place.
- **Delete Notes**: Remove unwanted notes with confirmation.
- **Persistance**: Notes are saved to a local SQLite database (`notes.db`).

## Tech Stack 🛠️

- **Backend**: Python 3.12 + Flask
- **Frontend**: Vue.js 3 (CDN), Tailwind CSS (CDN), FontAwesome
- **Database**: SQLite

## Setup & Run 🚀

1.  **Install Dependencies**:
    ```bash
    pip install flask flask-cors
    ```

2.  **Start the Application**:
    ```bash
    python app.py
    ```

3.  **Open in Browser**:
    Go to `http://localhost:5000`
